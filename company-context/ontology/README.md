# ontology/

Los tipos de entidad y de relación válidos están en
[`../SCHEMA.md`](../SCHEMA.md) (sección 3) y se validan con
[`../schemas/frontmatter.schema.json`](../schemas/frontmatter.schema.json)
— a propósito no se duplican acá en un archivo aparte (dos fuentes de
verdad para lo mismo se desincronizan solas).

Esta carpeta queda reservada para cuando haga falta algo que un
documento + JSON Schema no cubran — ej. un grafo real de relaciones
entre entidades para tooling (Fase 8) — no antes.
